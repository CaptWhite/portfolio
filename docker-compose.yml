services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio
    restart: always
    ports:
      - "8020:80"
    networks:
      - webnet-portfolio
    env_file:
      - ./.env

networks:
  webnet-portfolio:
    driver: bridge
 
